{
  "mcpServers": {
    "directus": {
      "url": "${env:NEXT_PUBLIC_DIRECTUS_URL}/mcp",
      "headers": {
        "Authorization": "Bearer ${env:DIRECTUS_ADMIN_TOKEN}"
      }
    },
    "trigger-dev": {
      "command": "npx",
      "args": [
        "trigger.dev@latest",
        "mcp"
      ],
      "env": {
        "TRIGGER_ACCESS_TOKEN": "${env:TRIGGER_SECRET_KEY}",
        "TRIGGER_API_URL": "${env:TRIGGER_API_URL}"
      }
    }
  }
}