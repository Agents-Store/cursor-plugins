{
  "mcpServers": {
    "dokploy": {
      "command": "npx",
      "args": [
        "-y",
        "@dokploy/mcp"
      ],
      "env": {
        "DOKPLOY_URL": "${env:DOKPLOY_URL}",
        "DOKPLOY_API_KEY": "${env:DOKPLOY_API_KEY}"
      }
    }
  }
}