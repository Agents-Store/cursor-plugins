{
  "mcpServers": {
    "nocobase": {
      "command": "npx",
      "args": [
        "-y",
        "@nocobase/mcp-server"
      ],
      "env": {
        "NOCOBASE_URL": "${env:NOCOBASE_URL}",
        "NOCOBASE_EMAIL": "${env:NOCOBASE_EMAIL}",
        "NOCOBASE_PASSWORD": "${env:NOCOBASE_PASSWORD}"
      }
    }
  }
}